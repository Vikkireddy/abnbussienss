export const STATUS_COLOR_MAP: Record<
  string,
  'success' | 'error' | 'default'
> = {
  active: 'success',
  cancelled: 'error',
};

export type StatusColorType = 'success' | 'error' | 'default';

